# Orchestration (Phase 3)

This folder contains the LangGraph setup for Phase 3-5.
- state.py defines FraudState and trace helpers.
- graph.py builds the sequential multi-agent graph used in Phase 5.

The graph is not wired into the API yet. That happens in Phase 7.
